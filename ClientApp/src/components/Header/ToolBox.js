import React, { Component } from 'react';

export class ToolBox extends Component {

  render() {
    return (
      <div>
        <p>This is a description of my toolbox, hosted on github pages.</p>

        <p>Some table or something should go here, offering links to specific places in my toolbox.</p>
      </div>
    );
  }
}